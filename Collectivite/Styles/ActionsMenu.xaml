<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <!--
        Menu d'actions de ligne partagé : un bouton "3 points verticaux" qui ouvre un
        popup listant les actions disponibles (icône + libellé), utilisé à la place
        d'une rangée de boutons icône dans les colonnes "Actions" des DataGrid.
    -->

    <Style x:Key="RowActionsPopupBoxStyle" TargetType="{x:Type materialDesign:PopupBox}"
           BasedOn="{StaticResource {x:Type materialDesign:PopupBox}}">
        <Setter Property="PlacementMode" Value="BottomAndAlignRightEdges"/>
        <Setter Property="StaysOpen" Value="False"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="ToggleContent">
            <Setter.Value>
                <materialDesign:PackIcon Kind="DotsVertical" Width="20" Height="20"/>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PopupMenuItemButtonStyle" TargetType="{x:Type Button}"
           BasedOn="{StaticResource MaterialDesignFlatButton}">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="MinWidth" Value="190"/>
        <Setter Property="MinHeight" Value="44"/>
        <Setter Property="Padding" Value="16,12"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="FontSize" Value="14"/>
    </Style>

</ResourceDictionary>
