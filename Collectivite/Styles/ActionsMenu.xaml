<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <!--
        Menu d'actions de ligne partagé : un bouton "3 points verticaux" qui ouvre un
        popup listant les actions disponibles (icône + libellé), utilisé à la place
        d'une rangée de boutons icône dans les colonnes "Actions" des DataGrid.
    -->

    <!--
        x:Shared="False" est indispensable ici : ce Style est fusionné au niveau App.xaml
        (donc partagé par toute l'application) et définit son ToggleContent via une
        instance de PackIcon directement dans le Setter.Value. Sans x:Shared="False",
        WPF ne renvoie qu'une seule instance de ce Style (et donc du PackIcon) pour
        tous les lookups StaticResource : un même UIElement ne pouvant être parenté
        qu'à un seul endroit à la fois, un seul PopupBox dans toute l'appli affichait
        réellement son icône "3 points" - tous les autres semblaient vides/inactifs.
    -->
    <Style x:Key="RowActionsPopupBoxStyle" x:Shared="False" TargetType="{x:Type materialDesign:PopupBox}"
           BasedOn="{StaticResource {x:Type materialDesign:PopupBox}}">
        <Setter Property="PlacementMode" Value="BottomAndAlignRightEdges"/>
        <Setter Property="StaysOpen" Value="False"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="ToggleContent">
            <Setter.Value>
                <materialDesign:PackIcon Kind="DotsVertical" Width="20" Height="20"/>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PopupMenuItemButtonStyle" TargetType="{x:Type Button}"
           BasedOn="{StaticResource MaterialDesignFlatButton}">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="MinWidth" Value="190"/>
        <Setter Property="MinHeight" Value="44"/>
        <Setter Property="Padding" Value="16,12"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="FontSize" Value="14"/>
    </Style>

</ResourceDictionary>
