<UserControl x:Class="Collectivite.Views.Pages.PDLPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:utils="clr-namespace:Collectivite.Utils"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
        <utils:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisConverter"/>
    </UserControl.Resources>

    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ═══════════════════════════════════════════════════════════ -->
        <!-- EN-TÊTE -->
        <!-- ═══════════════════════════════════════════════════════════ -->
        <Border Grid.Row="0" Background="White" Padding="20,15" 
                BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Programme de Développement Local" 
                               FontSize="24" FontWeight="Bold" Foreground="#1976D2"/>
                    <TextBlock Text="Gestion des PDL et planification des activités" 
                               FontSize="13" Foreground="#666666" Margin="0,4,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="#4CAF50"
                            Command="{Binding NouveauPDLCommand}"
                            Visibility="{Binding IsEditing, Converter={StaticResource InverseBoolToVisConverter}}"
                            Padding="16,8">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Plus" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="Nouveau PDL"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ═══════════════════════════════════════════════════════════ -->
        <!-- CONTENU PRINCIPAL -->
        <!-- ═══════════════════════════════════════════════════════════ -->
        <Grid Grid.Row="1" Margin="20">

            <!-- MODE LISTE -->
            <Grid Visibility="{Binding IsEditing, Converter={StaticResource InverseBoolToVisConverter}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Statistiques -->
                <Border Grid.Row="0" Background="#E3F2FD" CornerRadius="8" Padding="20" Margin="0,0,0,16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                            <TextBlock Text="Total PDL" FontSize="12" Foreground="#666666"/>
                            <TextBlock Text="{Binding TotalPDL, Mode=OneWay}" FontSize="28" FontWeight="Bold" Foreground="#1976D2"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                            <TextBlock Text="PDL Actuel" FontSize="12" Foreground="#666666"/>
                            <TextBlock FontSize="16" FontWeight="SemiBold" Foreground="#4CAF50">
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0:yyyy} - {1:yyyy}">
                                        <Binding Path="CurrentPDL.DateDebut"/>
                                        <Binding Path="CurrentPDL.DateFin"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </StackPanel>

                        <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                            <TextBlock Text="Statut" FontSize="12" Foreground="#666666"/>
                            <Border Background="#4CAF50" CornerRadius="12" Padding="12,4">
                                <TextBlock Text="En cours" Foreground="White" FontWeight="Bold"/>
                            </Border>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Liste des PDL -->
                <materialDesign:Card Grid.Row="1">
                    <DataGrid ItemsSource="{Binding PdlList}"
                              SelectedItem="{Binding SelectedPDL}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              materialDesign:DataGridAssist.CellPadding="16 12">

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Période" Width="180">
                                <DataGridTextColumn.Binding>
                                    <MultiBinding StringFormat="{}{0:dd/MM/yyyy} - {1:dd/MM/yyyy}">
                                        <Binding Path="DateDebut"/>
                                        <Binding Path="DateFin"/>
                                    </MultiBinding>
                                </DataGridTextColumn.Binding>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Durée" Width="100">
                                <DataGridTextColumn.Binding>
                                    <MultiBinding StringFormat="{}{0} ans">
                                        <Binding Path="DateFin.Year"/>
                                    </MultiBinding>
                                </DataGridTextColumn.Binding>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Description" 
                                                Binding="{Binding Description}" 
                                                Width="*"/>

                            <DataGridTextColumn Header="Fichier" 
                                                Binding="{Binding FicName}" 
                                                Width="150"/>

                            <DataGridTemplateColumn Header="Actions" Width="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Style="{StaticResource MaterialDesignIconButton}"
                                                    ToolTip="Modifier"
                                                    Command="{Binding DataContext.ModifierPDLCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                    CommandParameter="{Binding}">
                                                <materialDesign:PackIcon Kind="Pencil" Foreground="#1976D2"/>
                                            </Button>
                                            <Button Style="{StaticResource MaterialDesignIconButton}"
                                                    ToolTip="Supprimer"
                                                    Command="{Binding DataContext.SupprimerCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                    CommandParameter="{Binding}">
                                                <materialDesign:PackIcon Kind="Delete" Foreground="#F44336"/>
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </materialDesign:Card>
            </Grid>

            <!-- MODE ÉDITION -->
            <materialDesign:Card Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisConverter}}"
                                 Padding="24">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Titre formulaire -->
                    <TextBlock Grid.Row="0" Text="{Binding TitrePage}" 
                               FontSize="20" FontWeight="Bold" Foreground="#1976D2"
                               Margin="0,0,0,24"/>

                    <!-- Champs du formulaire -->
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Date début -->
                        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,12,16">
                            <TextBlock Text="Date de début *" FontWeight="SemiBold" Margin="0,0,0,8"/>
                            <DatePicker SelectedDate="{Binding DateDebut}"
                                        Style="{StaticResource MaterialDesignOutlinedDatePicker}"/>
                        </StackPanel>

                        <!-- Date fin -->
                        <StackPanel Grid.Row="0" Grid.Column="1" Margin="12,0,0,16">
                            <TextBlock Text="Date de fin *" FontWeight="SemiBold" Margin="0,0,0,8"/>
                            <DatePicker SelectedDate="{Binding DateFin}"
                                        Style="{StaticResource MaterialDesignOutlinedDatePicker}"/>
                        </StackPanel>

                        <!-- Description -->
                        <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,16">
                            <TextBlock Text="Description" FontWeight="SemiBold" Margin="0,0,0,8"/>
                            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     MinHeight="100"
                                     VerticalContentAlignment="Top"/>
                        </StackPanel>

                        <!-- Fichier joint -->
                        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,16">
                            <TextBlock Text="Fichier joint (optionnel)" FontWeight="SemiBold" Margin="0,0,0,8"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" 
                                         Text="{Binding FicName}"
                                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                         IsReadOnly="True"/>
                                <Button Grid.Column="1" 
                                        Style="{StaticResource MaterialDesignOutlinedButton}"
                                        Content="Parcourir..."
                                        Margin="8,0,0,0"/>
                            </Grid>
                        </StackPanel>
                    </Grid>

                    <!-- Boutons d'action -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
                        <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                                Content="Annuler"
                                Command="{Binding AnnulerCommand}"
                                Margin="0,0,12,0"
                                Padding="24,8"/>
                        <Button Style="{StaticResource MaterialDesignRaisedButton}"
                                Background="#4CAF50"
                                Content="Enregistrer"
                                Command="{Binding EnregistrerCommand}"
                                Padding="24,8"/>
                    </StackPanel>
                </Grid>
            </materialDesign:Card>
        </Grid>

        <!-- OVERLAY DE CHARGEMENT -->
        <Grid Grid.RowSpan="2" 
              Background="#80FFFFFF"
              Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                             IsIndeterminate="True" Width="50" Height="50"/>
                <TextBlock Text="Chargement..." Margin="0,16,0,0" Foreground="#666666"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
