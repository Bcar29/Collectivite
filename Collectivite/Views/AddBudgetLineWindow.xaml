<Window x:Class="Collectivite.Views.AddBudgetLineWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ajouter Ligne Budgetaire"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Nomenclature:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
        <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding AvailableNoms}" SelectedItem="{Binding SelectedNomenclature}" DisplayMemberPath="Intitule" Width="350" Margin="0,0,0,8"/>

        <TextBlock Text="Montant prévu:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Montant, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Annuler" Padding="8,4" Margin="0,0,8,0" Command="{Binding CancelCommand}"/>
            <Button Content="Créer" Padding="8,4" Command="{Binding CreateCommand}"/>
        </StackPanel>
    </Grid>
</Window>
